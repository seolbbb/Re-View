# 🪄 Optimized Prompt Design

## 🎯 Intent & Strategy

- **사용자 의도**: `screentime_mvp` 폴더 내의 파일들이 독립적인 스크립트들의 집합인지, 아니면 체계적인 패키지 구조를 갖추고 있는지 확인하고, 실행 가능한 상태(Runnable State)인지 점검받기를 원함.
- **적용된 기법**: Code Audit (코드 감사), Dependency Analysis (의존성 분석), Refactoring Proposal (리팩토링 제안).
- **예상 효과**: 현재 파일 구조의 문제점(예: 하드코딩된 경로, 누락된 `__init__.py`, 명확하지 않은 진입점 등)을 식별하고, 개선된 구조를 제안함.

## ✨ The Master Prompt (Copy & Paste this)

당신은 **Senior Python Developer**입니다.
`screentime_mvp` 디렉토리의 파일 구조와 코드 내용을 분석하여 **실행 가능성(Runnability)**과 **구조적 건전성(Structural Integrity)**을 진단하십시오.

### 1. 🔍 Analysis Scope

다음 파일들을 분석하십시오:

- `video_processor.py`
- `audio_processor.py`
- `mask_video.py`

### 2. 📝 Diagnosis Points

다음 항목들을 중점적으로 확인하십시오:

1. **Entry Point**: 명확한 실행 진입점(`if __name__ == "__main__":`)이 있는가?
2. **Dependencies**: 필요한 라이브러리(`requirements.txt`)가 명시되어 있거나 파악 가능한가?
3. **File Paths**: 입력/출력 경로가 하드코딩되어 있지 않은가? (상대 경로 또는 설정 파일 사용 여부)
4. **Module Structure**: 코드가 재사용 가능한 함수/클래스로 분리되어 있는가, 아니면 스크립트 형태인가?

### 3. 🛠️ Improvement Plan

분석 결과를 바탕으로 다음을 포함하는 `STRUCTURE_REVIEW.md`를 작성하십시오:

- **Current Status**: 현재 구조의 장단점 요약.
- **Issues**: 실행을 방해하는 요소(예: 절대 경로 사용, 모듈 import 에러 등).
- **Refactoring Proposal**: 더 나은 폴더 구조 제안 (예: `src/` 분리, `main.py` 생성 등).

### 📤 Output Deliverable

- **`STRUCTURE_REVIEW.md`** 파일 생성.
