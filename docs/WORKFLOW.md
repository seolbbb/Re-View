# 📊 Lecture-Note-AI 워크플로우 가이드

## 🎯 전체 시스템 구조

```
┌─────────────────────────────────────────────────────────────┐
│                    사용자 (User)                             │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      │ python src/process_content.py
                      ▼
┌─────────────────────────────────────────────────────────────┐
│              process_content.py (오케스트레이터)              │
│  - 입력 파일 스캔                                             │
│  - 모듈 초기화 및 호출                                        │
│  - 결과 저장 관리                                            │
└─────────┬───────────────────────────┬───────────────────────┘
          │                           │
          │ JSON 파싱                 │ 비디오 처리
          ▼                           ▼
┌─────────────────────┐    ┌──────────────────────────────────┐
│  JsonParser         │    │  VideoProcessor                  │
│  - JSON → 텍스트    │    │  - 장면 감지                      │
│  - 타임스탬프 정렬  │    │  - 키프레임 추출                  │
└─────────┬───────────┘    │  - 마우스 제거                    │
          │                │  - 중복 제거                      │
          │                └─────────┬────────────────────────┘
          │                          │
          ▼                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    출력 (Output)                             │
│  - *_readable.txt (텍스트)                                   │
│  - *_frames/ (슬라이드 이미지)                               │
│  - debug_scene_changes/ (디버그 이미지)                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 상세 워크플로우

### Phase 1: 초기화 및 스캔 (process_content.py)

```python
# 1. 디렉토리 설정
INPUT_DIR = "src/data/input"      # 영상 파일 위치
OUTPUT_DIR = "src/data/output"    # 결과 저장 위치

# 2. 파일 스캔
video_files = [*.mp4 파일들]
json_files = [*.json 파일들]

# 3. 모듈 초기화
json_parser = JsonParser()
video_processor = VideoProcessor()
```

**흐름도**:

```
시작
  │
  ├─> 입력 폴더 존재 확인
  │   └─> 없으면 생성
  │
  ├─> 출력 폴더 존재 확인
  │   └─> 없으면 생성
  │
  ├─> .mp4 파일 스캔
  │   └─> 리스트 생성
  │
  └─> .json 파일 스캔
      └─> 리스트 생성
```

---

### Phase 2: JSON 처리 (JsonParser)

```
JSON 파일 읽기
  │
  ├─> segments 배열 추출
  │
  ├─> 각 segment 처리
  │   ├─> start_time 추출
  │   ├─> text 추출
  │   └─> [MM:SS] 형식 변환
  │
  └─> 텍스트 파일 저장
      └─> {파일명}_readable.txt
```

**예시**:

```json
// 입력 (JSON)
{
  "segments": [
    {"start": 1500, "text": "안녕하세요"},
    {"start": 3000, "text": "강의를 시작하겠습니다"}
  ]
}

// 출력 (TXT)
[00:01] 안녕하세요
[00:03] 강의를 시작하겠습니다
```

---

### Phase 3: 비디오 처리 (VideoProcessor) ⭐ 핵심

#### 3-1. 메인 루프 (extract_keyframes)

```
비디오 열기
  │
  ├─> FPS, 총 프레임 수 확인
  │
  └─> 프레임 순회 시작
      │
      ├─> [첫 프레임]
      │   ├─> 다중 시점 샘플링 (0~6초)
      │   ├─> 마우스 제거
      │   └─> 저장
      │
      └─> [이후 프레임들]
          │
          ├─> 최소 간격 체크 (0.5초)
          │   └─> 간격 미달 → 스킵
          │
          ├─> 장면 전환 감지
          │   ├─> 이전 프레임과 픽셀 차이 계산
          │   ├─> mean_diff > threshold?
          │   │   └─> Yes: 장면 전환!
          │   │       │
          │   │       ├─> 슬라이드 경계 기록
          │   │       ├─> 마우스 제거 실행
          │   │       └─> 저장
          │   │
          │   └─> No: 다음 프레임
          │
          └─> 중복 제거 (dHash)
              └─> 최종 결과 반환
```

#### 3-2. 장면 전환 감지 상세

```python
# Step 1: 프레임 리사이즈 (성능 최적화)
current_frame_small = resize(frame, (640, 360))

# Step 2: 흑백 변환
current_gray = cvtColor(current_frame_small, GRAY)

# Step 3: 이전 프레임과 차이 계산
diff = absdiff(current_gray, previous_gray)

# Step 4: 평균 차이 계산
mean_diff = mean(diff)  # 0~255 범위

# Step 5: 임계값 비교
if mean_diff > threshold:  # 예: threshold=8
    → 장면 전환 감지!
```

**시각화**:

```
프레임 N-1        프레임 N         차이 계산
┌─────────┐      ┌─────────┐      ┌─────────┐
│ 슬라이드1│  →   │ 슬라이드2│  →   │ diff=45 │ → 장면 전환!
│  E=mc²  │      │  F=ma   │      │ (>8)    │
└─────────┘      └─────────┘      └─────────┘

프레임 N         프레임 N+1       차이 계산
┌─────────┐      ┌─────────┐      ┌─────────┐
│ 슬라이드2│  →   │ 슬라이드2│  →   │ diff=2  │ → 같은 장면
│  F=ma   │      │  F=ma ↖ │      │ (<8)    │
└─────────┘      └─────────┘      └─────────┘
         (마우스만 움직임)
```

#### 3-3. 마우스 제거 (Multi-Point Sampling)

```
슬라이드 구간 확인
  │
  ├─> 구간 >= 3초?
  │   │
  │   ├─> Yes: 다중 시점 샘플링
  │   │   │
  │   │   ├─> 슬라이드 전체 구간에서 50개 프레임 랜덤 수집
  │   │   │   예: [120초~240초] 구간
  │   │   │       → 125초, 133초, 147초, ... (50개)
  │   │   │
  │   │   ├─> 각 프레임 읽기
  │   │   │
  │   │   └─> Temporal Median 계산
  │   │       └─> 각 픽셀의 중간값 = 마우스 제거!
  │   │
  │   └─> No: 양방향 수집
  │       └─> [t-2초 ~ t+4초] 6초 수집
  │
  └─> 복원된 프레임 반환
```

**Temporal Median 원리**:

```
픽셀 (100, 200) 위치에서 50개 프레임의 값:

프레임 1:  15  (슬라이드 배경)
프레임 2:  14  (슬라이드 배경)
프레임 3:  200 (마우스 포인터!) ← 이상치
프레임 4:  13  (슬라이드 배경)
프레임 5:  16  (슬라이드 배경)
...
프레임 50: 14  (슬라이드 배경)

정렬: [13, 14, 14, 15, 16, ..., 200]
중간값(Median) = 14 ← 마우스 값(200)은 무시됨!
```

#### 3-4. 중복 제거 (dHash)

```
모든 캡처된 프레임
  │
  ├─> 각 프레임의 dHash 계산
  │   │
  │   ├─> 9x8 리사이즈
  │   ├─> 흑백 변환
  │   ├─> 가로 방향 차이 계산
  │   └─> 64비트 해시 생성
  │
  ├─> 프레임 간 해시 비교
  │   │
  │   ├─> Hamming Distance 계산
  │   │   (다른 비트 개수)
  │   │
  │   └─> Distance < 5?
  │       ├─> Yes: 중복으로 판단 → 제거
  │       └─> No: 유니크 → 유지
  │
  └─> 최종 유니크 프레임 반환
```

---

## 📊 데이터 흐름도

```
┌──────────────────────────────────────────────────────────────┐
│                    1. 입력 단계                               │
└──────────────────────┬───────────────────────────────────────┘
                       │
         ┌─────────────┴─────────────┐
         │                           │
         ▼                           ▼
┌─────────────────┐         ┌─────────────────┐
│  lecture.mp4    │         │  lecture.json   │
│  (20MB)         │         │  (50KB)         │
└────────┬────────┘         └────────┬────────┘
         │                           │
         │                           │
┌────────▼────────────────────────────▼────────────────────────┐
│                    2. 처리 단계                               │
│                                                               │
│  VideoProcessor              JsonParser                      │
│  ├─ 프레임 추출 (9개)        ├─ segments 파싱                │
│  ├─ 마우스 제거              ├─ 타임스탬프 변환              │
│  └─ 중복 제거 (→ 3개)       └─ 텍스트 정렬                  │
└───────────────────┬───────────────────────────────────────────┘
                    │
         ┌──────────┴──────────┐
         │                     │
         ▼                     ▼
┌─────────────────┐   ┌─────────────────┐
│  3개 이미지      │   │  텍스트 파일     │
│  - slide1.jpg   │   │  [00:01] ...    │
│  - slide2.jpg   │   │  [00:03] ...    │
│  - slide3.jpg   │   │  [00:05] ...    │
└─────────────────┘   └─────────────────┘
         │                     │
         └──────────┬──────────┘
                    ▼
┌──────────────────────────────────────────────────────────────┐
│                    3. 출력 단계                               │
│                                                               │
│  src/data/output/                                            │
│  ├─ lecture_frames/                                          │
│  │  ├─ frame_0000.00s.jpg                                   │
│  │  ├─ frame_0120.50s.jpg                                   │
│  │  └─ frame_0240.30s.jpg                                   │
│  ├─ lecture_frames/debug_scene_changes/                      │
│  │  └─ scene_change_*.jpg (9개)                             │
│  └─ lecture_readable.txt                                     │
└──────────────────────────────────────────────────────────────┘
```

---

## ⚙️ 파라미터 튜닝 가이드

### 1. threshold (장면 전환 민감도)

```python
# process_content.py에서 설정
video_processor.extract_keyframes(
    video_path,
    threshold=8,  # ← 여기 조정
    ...
)
```

**효과**:

```
threshold=5  → 매우 민감 (작은 변화도 감지)
  └─> 슬라이드 많이 캡처, 중복 가능성 높음

threshold=8  → 적당 (권장)
  └─> 명확한 슬라이드 전환만 감지

threshold=15 → 둔감 (큰 변화만 감지)
  └─> 슬라이드 누락 가능성 있음
```

### 2. min_interval (최소 캡처 간격)

```python
video_processor.extract_keyframes(
    video_path,
    min_interval=0.5,  # ← 여기 조정 (초 단위)
    ...
)
```

**효과**:

```
min_interval=0.3 → 빠른 전환 대응
  └─> 0.3초마다 캡처 가능

min_interval=0.5 → 적당 (권장)
  └─> 0.5초마다 캡처 가능

min_interval=2.0 → 느린 전환
  └─> 2초마다 캡처 가능
```

### 3. num_samples (샘플링 개수)

```python
# video_processor.py 내부
clean_frame = self._collect_and_reconstruct_multipoint(
    cap, slide_start, slide_end, fps,
    num_samples=50  # ← 여기 조정
)
```

**효과**:

```
num_samples=30 → 빠름, 마우스 제거율 낮음
num_samples=50 → 적당 (권장)
num_samples=100 → 느림, 마우스 제거율 높음
```

---

## 🐛 디버깅 가이드

### 문제 1: 슬라이드가 너무 많이 캡처됨

**원인 분석**:

```bash
# verbose=True로 실행
python src/process_content.py

# 로그 확인
[Diff Check] Time: 10.50s | Diff: 6.2 (Threshold: 8)
📸 Scene Change Detected at 10.53s (Diff: 8.5)  ← 작은 변화도 감지
[Diff Check] Time: 10.60s | Diff: 7.1 (Threshold: 8)
```

**해결**:

1. `threshold` 증가 (8 → 12)
2. `debug_scene_changes` 폴더에서 원본 확인

### 문제 2: 슬라이드가 누락됨

**원인 분석**:

```bash
# 로그 확인
[Diff Check] Time: 120.50s | Diff: 7.2 (Threshold: 8)  ← 임계값 미달
[Diff Check] Time: 240.30s | Diff: 7.8 (Threshold: 8)  ← 임계값 미달
```

**해결**:

1. `threshold` 감소 (8 → 5)
2. `verbose=True`로 Diff 값 모니터링

### 문제 3: 마우스가 남아있음

**원인 분석**:

```
슬라이드 구간: [120s ~ 125s] (5초)
  → 다중 시점 샘플링 적용 ✓

하지만 마우스가 한 곳에 4초 고정
  → 50개 프레임 중 40개에 마우스 포함
  → Median 계산 시 마우스 값이 중간값이 됨
```

**해결**:

1. `num_samples` 증가 (50 → 100)
2. 슬라이드 구간 확인 (너무 짧으면 양방향 수집 사용)

---

## 📝 코드 수정 예시

### 예시 1: 민감도 조정

```python
# src/process_content.py

# 변경 전
video_processor.extract_keyframes(
    video_path,
    threshold=8,  # 기본값
    min_interval=0.5
)

# 변경 후 (더 많은 슬라이드 캡처)
video_processor.extract_keyframes(
    video_path,
    threshold=5,  # 더 민감하게
    min_interval=0.3  # 더 자주 캡처
)
```

### 예시 2: 마우스 제거 강화

```python
# src/capture/video_processor.py

# 변경 전
clean_frame = self._collect_and_reconstruct_multipoint(
    cap, slide_start, slide_end, fps,
    num_samples=50
)

# 변경 후 (더 많은 샘플)
clean_frame = self._collect_and_reconstruct_multipoint(
    cap, slide_start, slide_end, fps,
    num_samples=100  # 2배 증가
)
```

---

## 🎯 성능 최적화 팁

### 1. 프레임 리사이즈

```python
# 640x360으로 리사이즈하여 계산 속도 4배 향상
curr_frame_small = cv2.resize(frame, (640, 360))
```

### 2. 샘플링 간격

```python
# 2프레임마다 1개만 수집 (속도 2배 향상)
sample_interval = 2
```

### 3. 조기 종료

```python
# 최소 간격 미달 시 즉시 스킵
if current_time - last_capture_time < min_interval:
    frame_idx += 1
    continue
```

---

## 📚 참고 자료

- **README.md**: 사용자 가이드
- **video_processor.py**: 코드 주석 (한글)
- **test_video_processor_simple.py**: 단위 테스트

---

## 💡 추가 개선 아이디어

1. **GPU 가속**: OpenCV CUDA 사용
2. **병렬 처리**: 여러 영상 동시 처리
3. **진행률 표시**: tqdm 라이브러리 활용
4. **자동 파라미터 조정**: 영상 특성 분석 후 자동 설정
