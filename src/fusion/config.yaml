paths:
  stt_json: "src/data/demo/stt.json"
  vlm_json: "src/data/demo/vlm.json"
  captures_manifest_json: "src/data/demo/manifest.json"
  output_root: "src/fusion/outputs"

sync_engine:
  boundary_source: "vlm_timestamps_visual_first"
  min_segment_sec: 15
  max_segment_sec: 90
  max_transcript_chars: 2000
  silence_gap_ms: 1500
  max_visual_items: 2
  max_visual_chars: 0
  dedup_similarity_threshold: 0.85

summarizer:
  bullets_per_segment_min: 1
  bullets_per_segment_max: 0
  claim_max_chars: 0
  temperature: 0.2
  json_repair_attempts: 0

llm_gemini:
  backend: "developer_api"
  model: "gemini-3-flash-preview"
  timeout_sec: 120
  max_retries: 5
  backoff_sec: [2, 5, 15, 30, 60]
  response_mime_type: "application/json"

  developer_api:
    api_key_env_candidates: ["GOOGLE_API_KEY", "GEMINI_API_KEY"]

  vertex_ai:
    auth_mode: "adc"
    project: null
    location: null
    api_key_env: "GOOGLE_API_KEY"

render:
  include_sources: false
  md_wrap_width: 0

final_summary:
  generate_formats: ["timeline", "tldr_timeline"]
  temperature: 0.2
  max_chars_per_format: 0
  style:
    include_timestamps: true
    language: "ko"
